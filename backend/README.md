# This is backend logic for the application
### Installation
1. you will need JDK 17 and maven 3
2. create twitter.json file based on twitter_example.json with your credentials 
3. run `mvn spring-boot:run`